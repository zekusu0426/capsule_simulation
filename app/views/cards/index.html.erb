<p id="notice"><%= notice %></p>

<h1>Cards</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Name kana</th>
      <th>Character</th>
      <th>Capsule</th>
      <th>Element</th>
      <th>Performance</th>
      <th>Technic</th>
      <th>Visual</th>
      <th>Introduction</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cards.each do |card| %>
      <tr>
        <td><%= card.name %></td>
        <td><%= card.name_kana %></td>
        <td><%= card.character_id %></td>
        <td><%= card.capsule_id %></td>
        <td><%= card.element_id %></td>
        <td><%= card.performance %></td>
        <td><%= card.technic %></td>
        <td><%= card.visual %></td>
        <td><%= card.introduction %></td>
        <td><%= link_to 'Show', card %></td>
        <td><%= link_to 'Edit', edit_card_path(card) %></td>
        <td><%= link_to 'Destroy', card, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Card', new_card_path %>
